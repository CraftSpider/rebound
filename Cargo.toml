[package]
name = "rebound"
version = "0.1.0"
authors = ["Rune Tynan <runetynan@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[features]
default = ["std"]

std = []
never-type = []

[dependencies]
rebound-proc = { path = "rebound-proc" }
ctor = "^0.1"
paste = "^1.0"
hashbrown = "^0.9"
